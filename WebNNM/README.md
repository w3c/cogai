# WebNNM for Browser AI
WebNNM is a high level file format for neural network models for use with the JavaScript module `webnnm.mjs` for inference, testing and training models in web browsers using WebNN as a platform independent backend utilising the NPU, GPU or CPU. The module is under 3000 lines of JavaScript and very much smaller and easier to use than existing WebNN libraries.
